const encryptor = require("../utils/encrypt");
const generateJWT = require("../utils/jwt");
const { Admin, Cafeteria } = require("../models");

// 로그인
class AdminService {
  constructor() {}

  login(adminId, password, attributes) {}
}
